import { environment } from '../../environments/environment';

export const port_clientes = environment.port_clientes;
export const port_sunat = environment.port_sunat;
export const port_clientes_onpremise = environment.port_clientes_onpremise;